# 🏗️ Arquitectura del Sistema

## Visión General

El sistema sigue una **arquitectura en capas** con separación clara de responsabilidades, permitiendo:

- **Escalabilidad**: Cada capa puede escalar independientemente
- **Mantenibilidad**: Cambios en una capa no afectan otras
- **Extensibilidad**: Fácil agregar nuevos canales (WhatsApp, Instagram, etc.)
- **Testabilidad**: Cada componente puede probarse de forma aislada

---

## Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CAPA EXTERNA (BFF)                               │
│   Backend for Frontend - Adaptadores para canales de comunicación       │
├─────────────────┬─────────────────┬─────────────────────────────────────┤
│                 │                 │                                     │
│  ┌───────────┐  │  ┌───────────┐  │  ┌───────────┐                      │
│  │ Telegram  │  │  │ WhatsApp  │  │  │ Instagram │  (futuro)            │
│  │    BFF    │  │  │    BFF    │  │  │    BFF    │                      │
│  └─────┬─────┘  │  └─────┬─────┘  │  └─────┬─────┘                      │
│        │        │        │        │        │                            │
└────────┼────────┴────────┼────────┴────────┼────────────────────────────┘
         │                 │                 │
         ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        CAPA API (FastAPI)                               │
│              Endpoints REST - Punto de entrada unificado                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐        │
│  │    /chat    │ │ /properties │ │   /leads    │ │/appointments│        │
│  │  (messages) │ │  (search)   │ │             │ │             │        │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘        │
│         │               │               │               │               │
└─────────┼───────────────┼───────────────┼───────────────┼───────────────┘
          │               │               │               │
          ▼               ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        CAPA DE SERVICIOS                                │
│                   Lógica de negocio y orquestación                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                      AI MODULE                                    │   │
│  │  ┌────────────────┐  ┌─────────────┐  ┌─────────────────────┐    │   │
│  │  │  Orchestrator  │──│  Onboarding │  │  PropertySearch     │    │   │
│  │  │     Agent      │  │    Agent    │  │      Agent          │    │   │
│  │  └───────┬────────┘  └─────────────┘  └──────────┬──────────┘    │   │
│  │          │                                       │               │   │
│  │          │           ┌─────────────┐             │               │   │
│  │          └───────────│  Schedule   │─────────────┘               │   │
│  │                      │    Agent    │                             │   │
│  │                      └─────────────┘                             │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐      │
│  │ EmbeddingService│  │  SearchService  │  │ AppointmentService  │      │
│  │   (OpenAI)      │  │    (RAG)        │  │                     │      │
│  └────────┬────────┘  └────────┬────────┘  └─────────────────────┘      │
│           │                    │                                        │
└───────────┼────────────────────┼────────────────────────────────────────┘
            │                    │
            ▼                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        CAPA DE DATOS                                    │
│              Persistencia y caché                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────┐    ┌─────────────────────────────┐     │
│  │        PostgreSQL           │    │          Redis               │     │
│  │        (pgvector)           │    │         (Cache)              │     │
│  │                             │    │                              │     │
│  │  ┌─────────┐ ┌───────────┐  │    │  ┌───────────────────────┐  │     │
│  │  │ projects│ │properties │  │    │  │ conversation_history  │  │     │
│  │  └─────────┘ └───────────┘  │    │  │   (últimos N msgs)    │  │     │
│  │  ┌─────────┐ ┌───────────┐  │    │  └───────────────────────┘  │     │
│  │  │  leads  │ │  messages │  │    │  ┌───────────────────────┐  │     │
│  │  └─────────┘ └───────────┘  │    │  │    search_cache       │  │     │
│  │  ┌───────────────────────┐  │    │  │ (resultados búsqueda) │  │     │
│  │  │     appointments      │  │    │  └───────────────────────┘  │     │
│  │  └───────────────────────┘  │    │                              │     │
│  └─────────────────────────────┘    └─────────────────────────────┘     │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                      REPOSITORIES                                │    │
│  │   LeadRepository │ ConversationRepository │ PropertyRepository   │    │
│  │   MessageRepository │ AppointmentRepository │ ProjectRepository  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Patrones de Diseño Utilizados

### 1. **Repository Pattern**
Abstrae el acceso a datos, permitiendo cambiar la implementación sin afectar la lógica de negocio.

```python
# Ejemplo
class PropertyRepository:
    async def find_by_id(self, id: UUID) -> Property | None
    async def search_with_embedding(self, embedding: list[float], limit: int) -> list[Property]
    async def create(self, data: PropertyCreate) -> Property
```

### 2. **Dependency Injection**
FastAPI inyecta dependencias automáticamente, facilitando testing y modularidad.

```python
@router.get("/properties/{id}")
async def get_property(
    id: UUID,
    repo: PropertyRepository = Depends(get_property_repository)
):
    return await repo.find_by_id(id)
```

### 3. **Strategy Pattern (Agentes)**
Cada agente implementa una estrategia específica para manejar diferentes tipos de intenciones.

```python
class BaseAgent(ABC):
    @abstractmethod
    async def handle(self, message: str, context: ConversationContext) -> AgentResponse
```

### 4. **Factory Pattern (Agent Orchestrator)**
El orquestador crea y selecciona el agente apropiado según la clasificación del mensaje.

### 5. **Adapter Pattern (BFF)**
Cada BFF adapta las APIs externas (Telegram, WhatsApp) al formato interno del sistema.

---

## Flujo de una Conversación

```
1. Usuario envía mensaje en Telegram
                │
                ▼
2. Telegram BFF recibe webhook
                │
                ▼
3. BFF llama a POST /api/chat/message
   con { lead_id, message, channel: "telegram" }
                │
                ▼
4. API valida y pasa al AI Module
                │
                ▼
5. Orchestrator clasifica la intención:
   - ONBOARDING_SMALL_TALK → OnboardingAgent
   - PROPERTY_SEARCH → PropertySearchAgent
   - SCHEDULE_VISIT → ScheduleAgent
                │
                ▼
6. Agente correspondiente procesa:
   - Consulta Redis para historial
   - Consulta PostgreSQL si necesario
   - Genera respuesta con LLM
                │
                ▼
7. Respuesta estructurada se guarda en DB
                │
                ▼
8. API retorna respuesta al BFF
                │
                ▼
9. BFF formatea y envía a Telegram
```

---

## Modelo de Datos

### Diagrama ER Simplificado

```
┌─────────────┐       ┌───────────────┐       ┌─────────────┐
│    leads    │───────│ conversations │───────│  messages   │
│             │ 1   N │               │ 1   N │             │
│ - id        │       │ - id          │       │ - id        │
│ - name      │       │ - lead_id     │       │ - type      │
│ - email     │       │ - project_id  │       │ - content   │
│ - phone     │       │ - created_at  │       │ - conv_id   │
│ - telegram_ │       └───────────────┘       └─────────────┘
│   chat_id   │
└─────────────┘
       │
       │ 1
       │
       │ N
┌─────────────┐       ┌───────────────┐       ┌─────────────┐
│appointments │───────│   projects    │───────│ properties  │
│             │ N   1 │               │ 1   N │             │
│ - id        │       │ - id          │       │ - id        │
│ - lead_id   │       │ - name        │       │ - title     │
│ - project_id│       │ - district    │       │ - pricing   │
│ - scheduled │       │ - embedding   │       │ - embedding │
└─────────────┘       └───────────────┘       │ - project_id│
                                              │ - typology_ │
                             ┌────────────────│   id        │
                             │                └─────────────┘
                             │ N
                             │
                             │ 1
                      ┌─────────────┐
                      │ typologies  │
                      │             │
                      │ - id        │
                      │ - name      │
                      │ - bedrooms  │
                      │ - bathrooms │
                      │ - area_m2   │
                      └─────────────┘
```

---

## Decisiones de Arquitectura

| Decisión | Justificación |
|----------|---------------|
| **FastAPI** | Async nativo, auto-documentación OpenAPI, tipado fuerte |
| **SQLAlchemy 2.0 Async** | ORM maduro con soporte async, migrations con Alembic |
| **Repository Pattern** | Desacopla lógica de negocio del acceso a datos |
| **BFF separado** | Permite agregar canales sin modificar el core |
| **Redis para cache** | Rápido, simple, ideal para sesiones y cache |
| **pgvector** | Búsqueda vectorial nativa en PostgreSQL |

---

*Última actualización: Diciembre 2024*

